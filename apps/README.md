# apps

Monorepo applications directory.

- `api`: backend service (from current `wx-markdown`)
- `web`: browser editor app (from current `md2weixin`)

Developer docs:

- `../docs/DEVELOPER.md`
- `../docs/monorepo-migration.md`
